<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profesores y sus Materias</title>
</head>

<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    .cuerpo {
        background-color: rgb(43, 43, 43);
    }

    .titulo {
        background-color: rgb(207, 207, 207);
        width: 100%;
        height: 10vh;

        font-size: 46px;
        color: black;
        display: flex;
        align-items: center;
        justify-content: center;
    }
    .titulo b {
        color: rgb(0, 139, 204);
    }
    .encabezado {
        margin-top: 25px;
        margin-left: 25px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
        

        background-color: bisque;
        border-radius: 20px;
        border: 2px solid black
    }
    .encabezado strong {
        font-size: 30px;
    }
    .lista {
        margin: 25px;
        padding: 40px;
        gap: 20px;
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        background: rgb(29, 29, 29);
        border-radius: 25px;
        list-style: none;
    }
    .tarjeta {
        background-color: bisque;
        border-radius: 15px;
        display: flex;
        flex-direction: column;
        padding: 20px;
        gap: 5px;
    }
    .subtitulo {
        color: rgb(0, 139, 204);
        font-size: 30px;
    }
</style>

<body class="cuerpo">
    <h1 class="titulo">Profesores y sus Materias</h1>
    <% if (profesores && profesores.length > 0) { %>
        <% profesores.forEach(profesor => { %>
            <div>
                <h2 class="encabezado"><Strong><%= profesor.profesorAsociado.nombre %> <%= profesor.profesorAsociado.apellido %></Strong></h2>
                <% if (profesor.materiasAsociadas && profesor.materiasAsociadas.length > 0) { %>
                    <ul class="lista">
                        <% profesor.materiasAsociadas.forEach(materia => { %>
                            <li class="tarjeta">
                                <strong>Materia:</strong> <%= materia.materia %><br>
                                <strong>Descripción:</strong> <%= materia.descripcion %>
                            </li>
                        <% }) %>
                    </ul>
                <% } else { %>
                    <p class="lista subtitulo">Este profesor no tiene materias asociadas.</p>
                <% } %>
            </div>
        <% }) %>
    <% } else { %>
        <p>No hay profesores registrados o no tienen materias asociadas.</p>
    <% } %>
</body>
</html>
